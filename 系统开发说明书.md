# 考试系统开发说明书

## 项目信息

**项目名称**: 考试系统 (Online Examination System)  
**开发者**: Li zekun  
**技术栈**: Flask + Vue.js + Docker  
**创建日期**: 2026-04-08  
**版本**: v1.0.0

---

## 开发日志

### 2026-04-08 开发记录

#### 1. Nginx 默认首页修复

**问题**: Nginx 启动后显示 403 Forbidden，无默认首页

**解决方案**:
- 创建默认 index.html 文件于 frontend/dist 目录
- 编写一键修复脚本 fix-nginx-homepage.sh
- 确保 Nginx 容器正确挂载静态文件目录

**修改文件**:
- `frontend/dist/index.html` - 创建默认首页
- `fix-nginx-homepage.sh` - 修复脚本

**技术要点**:
- Nginx 配置静态文件路径：/usr/share/nginx/html
- Docker 卷挂载：./frontend/dist:/usr/share/nginx/html
- 前端构建前提供临时首页

---

#### 2. 登录界面开发

**功能需求**:
- 用户输入框
- 密码输入框
- 日期选择框
- 注册按钮
- 登录按钮

**界面设计**:
- 采用 flex 布局固定标签位置
- 标签格式：用户：、密码：、日期：
- 标签宽度固定 60px，右对齐
- 移除多余动画效果

**修改文件**:
- `frontend/src/views/auth/Login.vue` - 登录页面组件

**技术实现**:
```vue
.form-item {
  display: flex;
  align-items: center;
}

.form-label {
  width: 60px;
  text-align: right;
}
```

---

#### 3. 系统导航界面开发

**布局结构**: 上中下三部分

**上部布局** (1/30 高度):
- 系统 Logo 和名称
- 欢迎信息
- 当前日期时间
- 退出按钮

**中间布局** (左右分割):
- 左侧导航菜单 (1/10 宽度):
  - 📊 驾驶 BI
  - 📚 学习中心
  - 📝 试卷中心
  - ✏️ 考试中心
  - ⚙️ 系统设置
- 右侧内容区：菜单窗口浏览区

**下部布局** (1/50 高度):
- 版权信息
- 技术支持

**修改文件**:
- `frontend/src/components/Layout.vue` - 系统布局组件
- `frontend/src/views/Home.vue` - 首页组件
- `frontend/src/views/BI.vue` - 驾驶 BI 页面
- `frontend/src/views/Study.vue` - 学习中心页面
- `frontend/src/views/Paper.vue` - 试卷中心页面
- `frontend/src/views/Exam.vue` - 考试中心页面
- `frontend/src/views/System.vue` - 系统设置页面
- `frontend/src/router/index.js` - 路由配置
- `frontend/src/App.vue` - 主应用组件

**技术要点**:
- 使用 Vue Router 进行路由管理
- Vuex 状态管理
- 路由守卫实现认证
- 响应式布局设计

---

#### 4. 默认首页优化

**功能需求**:
- 显示系统 Logo 和标题
- 提供登录和注册入口
- 自动检测后端健康状态
- 不显示敏感信息

**界面设计**:
- 简洁的卡片式布局
- 渐变背景
- 加载动画（后端检测）
- 移除"系统正常运行"提示

**修改文件**:
- `frontend/dist/index.html` - 默认首页

**技术实现**:
- 每 3 秒检测一次后端健康状态
- 加载动画自动隐藏
- 简洁的按钮设计

---

#### 5. 登录界面优化

**优化内容**:
- 移除系统正常运行提示
- 移除管理员登录文字
- 简化界面元素
- 固定标签位置

**界面布局**:
```
┌─────────────────────────┐
│         📚              │
│       考试系统           │
│  Online Examination     │
│                         │
│   用户：[________]      │
│   密码：[________]      │
│   日期：[2026-04-08]    │
│                         │
│   [注册]    [登录]      │
└─────────────────────────┘
```

**修改文件**:
- `frontend/src/views/auth/Login.vue` - 登录页面

---

## 文件结构

```
ai_softomanger/
├── frontend/
│   ├── dist/                      # 生产构建输出目录
│   │   └── index.html            # 默认首页
│   └── src/
│       ├── views/
│       │   ├── auth/
│       │   │   ├── Login.vue     # 登录页面
│       │   │   └── Register.vue  # 注册页面
│       │   ├── Home.vue          # 首页
│       │   ├── BI.vue            # 驾驶 BI
│       │   ├── Study.vue         # 学习中心
│       │   ├── Paper.vue         # 试卷中心
│       │   ├── Exam.vue          # 考试中心
│       │   └── System.vue        # 系统设置
│       ├── components/
│       │   └── Layout.vue        # 系统布局
│       ├── router/
│       │   └── index.js          # 路由配置
│       ├── store/
│       │   └── index.js          # Vuex 状态管理
│       ├── api/
│       │   └── index.js          # API 接口
│       ├── App.vue               # 主应用
│       └── main.js               # 入口文件
├── exam_system/
│   ├── app.py                    # Flask 应用
│   └── ...                       # 后端代码
├── nginx/
│   └── nginx.conf                # Nginx 配置
├── docker-compose.yml            # Docker 编排
├── Dockerfile                    # Docker 镜像
└── 系统开发说明书.md              # 本文档
```

---

## 技术架构

### 前端技术栈
- **框架**: Vue.js 3
- **路由**: Vue Router 4
- **状态管理**: Vuex 4
- **UI 库**: Element Plus
- **构建工具**: Vite
- **样式**: CSS3 + Flexbox

### 后端技术栈
- **框架**: Flask
- **ORM**: SQLAlchemy
- **认证**: JWT
- **缓存**: Redis
- **数据库**: SQLite/MySQL

### 部署架构
- **容器化**: Docker
- **服务编排**: Docker Compose
- **反向代理**: Nginx
- **应用服务器**: Gunicorn

---

## 路由配置

| 路径 | 组件 | 说明 | 认证 |
|------|------|------|------|
| `/` | 重定向 | 重定向到/home | - |
| `/home` | Home | 系统首页 | ✅ |
| `/login` | Login | 登录页面 | ❌ |
| `/register` | Register | 注册页面 | ❌ |
| `/bi` | BI | 驾驶 BI | ✅ |
| `/study` | Study | 学习中心 | ✅ |
| `/paper` | Paper | 试卷中心 | ✅ |
| `/exam` | Exam | 考试中心 | ✅ |
| `/system` | System | 系统设置 | ✅ |

---

## 布局设计

### 登录布局
- 居中卡片设计
- 渐变背景
- 固定标签位置
- 简洁的表单元素

### 系统布局
- **上**: Header (1/30 高度)
  - Logo、欢迎信息、日期、退出按钮
- **中**: 主体内容
  - **左**: 导航菜单 (1/10 宽度)
  - **右**: 内容区域
- **下**: Footer (1/50 高度)
  - 版权信息

---

## 开发规范

### 代码注释
- 所有代码文件使用 UTF-8 编码
- 注释使用中文
- 文件头部包含作者信息

### 文件头部格式
```javascript
/**
 * 文件名
 * 描述：文件功能描述
 * 作者：Li zekun
 * 创建日期：2026-04-08
 */
```

### 组件命名
- 使用 PascalCase 命名组件
- 使用 kebab-case 命名文件
- 路由名称使用大驼峰

### 样式规范
- 使用 scoped CSS
- 优先使用 flex 布局
- 避免硬编码尺寸，使用相对单位

---

## 部署流程

### 本地开发
```bash
cd frontend
npm install
npm run dev
```

### 生产构建
```bash
cd frontend
npm run build
```

### Docker 部署
```bash
docker-compose down
docker-compose up -d --build
```

---

## 常见问题

### Nginx 403 错误
**原因**: 静态文件目录为空或权限问题  
**解决**: 创建默认 index.html 并确保正确挂载

### 容器间网络通信
**配置**: 使用专用 Docker 网络 exam-network  
**通信**: 使用服务名进行容器间通信

### 路由守卫
**实现**: 在 router/index.js 中配置 beforeEach 守卫  
**逻辑**: 检查 token，决定路由跳转

---

#### 6. 自动化部署脚本开发

**功能需求**:
- 自动提交代码到 Git 仓库
- 自动连接到阿里云服务器
- 自动拉取最新代码
- 自动重启 Docker 服务
- 自动验证部署状态

**技术实现**:
- 创建 PowerShell 脚本（Windows 环境）
- 创建 Bash 脚本（Linux 环境）
- 使用 SSH 连接服务器
- 使用 Git 进行代码同步
- 使用 Docker Compose 管理服务

**修改文件**:
- `deploy.ps1` - Windows 自动化部署脚本（推荐使用）
- `auto-deploy.ps1` - Windows 备用部署脚本
- `auto-deploy.sh` - Linux 自动化部署脚本
- `deploy.config.example` - 部署配置文件模板
- `.gitignore` - Git 忽略文件配置

**技术要点**:
- SSH 自动连接（使用 OpenSSH）
- Git 自动提交和推送
- Docker 服务自动重启
- 部署状态自动验证
- 配置文件模板（避免敏感信息泄露）

**使用方法**:
```powershell
# Windows 环境（推荐）
.\deploy.ps1

# Windows 环境（备用）
.\auto-deploy.ps1

# Linux 环境
bash auto-deploy.sh
```

**部署流程**:
1. 检查代码变更并提交到 Git
2. 推送到远程 Git 仓库（GitHub）
3. SSH 连接到阿里云服务器
4. 拉取最新代码
5. 停止现有 Docker 服务
6. 重新构建并启动服务
7. 验证容器状态和日志

**服务器配置**:
- 服务器地址：47.93.44.247
- 部署路径：/opt/ai_softomanger
- 服务端口：80
- Git 仓库：github.com:645057072/ai_softomanger.git

**配置文件**:
- `deploy.config.example` - 配置文件模板
- 复制为 `deploy.config` 并填写实际配置
- `deploy.config` 已添加到 `.gitignore`，不会提交到 Git

**Git 配置**:
- 创建 `.gitignore` 文件
- 忽略敏感信息文件（密码、密钥等）
- 忽略临时文件和日志文件
- 忽略依赖目录（node_modules、__pycache__ 等）

**注意事项**:
1. 首次使用需要配置 SSH 密钥或输入密码
2. 确保本地已安装 Git 和 OpenSSH 客户端
3. 确保服务器已安装 Git 和 Docker
4. 配置文件中的敏感信息不要提交到 Git

---

## 下一步计划

1. 完善各功能模块页面内容
2. 实现数据统计和图表展示
3. 添加题目管理功能
4. 实现试卷组卷功能
5. 实现在线考试功能
6. 完善成绩管理功能
7. 优化用户交互体验
8. 增强系统安全性

---

## 版本历史

| 版本 | 日期 | 更新内容 | 开发者 |
|------|------|----------|--------|
| v1.0.0 | 2026-04-08 | 初始版本，完成基础架构和界面 | Li zekun |
| v1.0.1 | 2026-04-08 | 添加自动化部署脚本，优化开发流程 | Li zekun |
| v1.0.2 | 2026-04-08 | 完善部署脚本，添加配置模板和使用说明 | Li zekun |

---

## 部署说明

### 自动化部署

**使用自动化脚本进行一键部署**，脚本会自动完成以下操作：
1. 提交代码到 Git 仓库
2. 连接到阿里云服务器
3. 拉取最新代码
4. 重启 Docker 服务
5. 验证部署状态

**Windows 环境**:
```powershell
.\auto-deploy.ps1
```

**Linux 环境**:
```bash
bash auto-deploy.sh
```

### 手动部署

如果自动化脚本无法使用，可以按照以下步骤手动部署：

#### 1. 提交代码到 Git
```bash
git add .
git commit -m "提交说明"
git push origin main
```

#### 2. 连接到服务器
```bash
ssh root@47.93.44.247
```

#### 3. 拉取代码
```bash
cd /opt/ai_softomanger
git pull origin main
```

#### 4. 重启服务
```bash
docker-compose down
docker-compose up -d --build
```

#### 5. 查看日志
```bash
docker-compose logs -f
```

### 访问地址

- **前端首页**: http://47.93.44.247
- **登录页面**: http://47.93.44.247/login
- **系统主页**: http://47.93.44.247/home

---

**文档维护**: 每次开发后及时更新此文档  
**文档作者**: Li zekun  
**最后更新**: 2026-04-09

---

## 2026-04-09 开发记录（补充）

### 2. 修复登录认证问题 - Token 认证机制完善

**问题**: 组织机构管理新增时提示"无效的 Token"，所有表单增删改查操作失败

**问题原因**:
1. **前端使用模拟登录**: `Login.vue` 组件没有调用真实后端 API，而是使用 `userStore.login('mock-token', ...)` 模拟登录
2. **Token 无效**: 存储的 `'mock-token'` 不是有效的 JWT Token，后端无法验证
3. **认证失败**: 所有需要 `@jwt_required()` 验证的 API 请求都返回 401 UNAUTHORIZED

**Token 认证机制说明**:
- **用户权限认证**: 验证用户身份和角色（admin, user）
- **API 接口认证**: 所有增删改查操作都需要有效的 JWT Token
- **双重保护**: 既要验证用户权限，也要验证 API 访问令牌

**解决方案**:
1. 修改 `Login.vue` 调用真实后端登录 API `/api/auth/login`
2. 存储后端返回的 `access_token` 到 localStorage
3. 前端 API 请求使用存储的 Token 进行认证
4. 统一 Token 键名为 `token`

**修改文件**:
- `frontend/src/views/auth/Login.vue` - 登录页面（核心修复）
- `frontend/src/views/admin/Organization.vue` - 组织机构管理
- `frontend/src/views/admin/UserManagement.vue` - 用户管理
- `frontend/src/views/admin/UserApproval.vue` - 用户审核
- `frontend/src/views/admin/Role.vue` - 角色管理
- `frontend/src/views/Home.vue` - 首页
- `frontend/src/components/Layout.vue` - 布局组件

**技术实现**:
```javascript
// 登录时调用真实 API
async handleLogin() {
  const response = await fetch('/api/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username, password })
  })
  
  const result = await response.json()
  
  if (result.code === 200) {
    // 存储后端返回的 access_token
    userStore.login(result.data.access_token, result.data.user)
  }
}

// API 请求时携带 Token
const response = await fetch('/api/organization', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${localStorage.getItem('token')}`
  },
  body: JSON.stringify(formData)
})
```

**影响范围**:
- ✅ 所有需要认证的表单新增功能
- ✅ 所有需要认证的表单编辑功能
- ✅ 所有需要认证的表单删除功能
- ✅ 所有需要认证的列表查询功能
- ✅ 组织机构管理、用户管理、用户审核、角色管理等所有模块

**验证结果**:
- ✅ 管理员登录成功，获取有效 JWT Token
- ✅ Token 正确存储到 localStorage
- ✅ API 请求携带有效 Token
- ✅ 后端验证 Token 通过
- ✅ 组织机构新增成功
- ✅ 所有表单、列表的增删改查功能正常

**相关文档**:
- `TOKEN 认证机制说明.md` - 详细的 Token 认证机制文档

---

**文档维护**: 每次开发后及时更新此文档  
**文档作者**: Li zekun  
**最后更新**: 2026-04-09
