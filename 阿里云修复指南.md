# 阿里云服务器修复指南

**服务器信息**:
- IP: 47.93.44.247
- 用户：root
- 目录：/opt/ai_softomanger

## 问题诊断

根据截图显示，主要问题是：
1. Nginx 配置监听 localhost，外部无法访问
2. 阿里云安全组可能未开放 80 端口
3. 容器网络配置需要优化

## 修复步骤

### 步骤 1: 登录服务器

```bash
# 在本地 PowerShell 执行
ssh root@47.93.44.247
# 输入密码：AAAAa@321
```

### 步骤 2: 停止当前服务

```bash
cd /opt/ai_softomanger
docker-compose down
```

### 步骤 3: 检查 Nginx 配置是否已更新

```bash
# 查看 nginx.conf 文件
cat nginx/nginx.conf | grep "listen"
```

**应该看到**: `listen 0.0.0.0:80;`
**如果还是**: `listen 80;` 或 `listen localhost:80;`，需要手动修改

### 步骤 4: 手动修正 Nginx 配置（如果需要）

```bash
# 备份原配置
cp nginx/nginx.conf nginx/nginx.conf.bak

# 使用 sed 修改监听地址
sed -i 's/listen 80;/listen 0.0.0.0:80;/g' nginx/nginx.conf
sed -i 's/server_name localhost;/server_name _;/g' nginx/nginx.conf

# 验证修改
cat nginx/nginx.conf | grep -A 2 "server {"
```

### 步骤 5: 重新构建和启动

```bash
# 重新构建并启动
docker-compose up -d --build

# 查看启动日志
docker-compose logs -f
```

### 步骤 6: 检查服务状态

```bash
# 查看所有容器状态
docker-compose ps

# 应该看到所有容器状态为 Up
```

### 步骤 7: 测试访问

```bash
# 在服务器本地测试
curl -I http://localhost

# 测试后端 API
curl http://localhost/api/system/health
```

### 步骤 8: 配置阿里云安全组（关键！）

**必须在阿里云控制台操作**:

1. 打开浏览器访问：https://ecs.console.aliyun.com/
2. 找到实例：47.93.44.247
3. 点击"安全组"选项卡
4. 点击"配置规则"
5. 添加入方向规则：
   - 端口范围：80/80
   - 授权对象：0.0.0.0/0
   - 优先级：1
   - 策略：允许

### 步骤 9: 验证外部访问

```bash
# 在本地 PowerShell 测试（新开一个终端）
curl http://47.93.44.247
curl http://47.93.44.247/api/system/health
```

## 快速修复脚本

在服务器上执行以下命令：

```bash
# 1. 停止服务
cd /opt/ai_softomanger
docker-compose down

# 2. 修正 Nginx 配置
cat > nginx/nginx.conf << 'EOF'
events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;
    error_log   /var/log/nginx/error.log   warn;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 0.0.0.0:80;
        server_name _;

        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        location /api {
            proxy_pass http://backend:5001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /socket.io {
            proxy_pass http://backend:5001;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            proxy_read_timeout 86400s;
            proxy_send_timeout 86400s;
        }

        error_page  404              /404.html;
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}
EOF

# 3. 重新启动
docker-compose up -d --build

# 4. 查看日志
docker-compose logs -f
```

## 验证清单

- [ ] Nginx 配置已修改为 `listen 0.0.0.0:80`
- [ ] 所有容器状态正常（docker-compose ps）
- [ ] 服务器本地可以访问（curl http://localhost）
- [ ] 阿里云安全组 80 端口已开放
- [ ] 外部可以访问（浏览器访问 http://47.93.44.247）

## 常见错误处理

### 错误 1: 容器启动失败

```bash
# 查看错误日志
docker logs exam-system-backend

# 常见问题：数据库文件不存在
# 解决：确保 exam_system 目录存在
ls -lh exam_system/
```

### 错误 2: Nginx 配置错误

```bash
# 测试 Nginx 配置
docker exec exam-system-nginx nginx -t

# 重新加载 Nginx 配置
docker exec exam-system-nginx nginx -s reload
```

### 错误 3: 仍然无法访问

```bash
# 检查 80 端口监听
netstat -tlnp | grep :80

# 检查防火墙
systemctl status firewalld

# 如果防火墙开启，临时关闭测试
systemctl stop firewalld
```

## 完成后的访问地址

- **前端**: http://47.93.44.247
- **后端 API**: http://47.93.44.247/api
- **健康检查**: http://47.93.44.247/api/system/health
- **管理员账户**: admin / admin123
